# config.yaml

start_date: "2024-01-01"
max_results: 150

categories:
  foundation_model:
    queries:
      - ["foundation model", "remote sensing"]
      - ["foundation model", "geospatial"]
      - ["foundation model", "ocean"]
      - ["geospatial", "embedding"]
  
  remote_sensing:
    queries:
      - ["remote sensing", "deep learning"]
      - ["remote sensing", "machine learning"]

  ocean:
    queries:
      - ["ocean", "deep learning"]
      - ["ocean", "machine learning"]
      - ["ocean", "surface currents"]
      - ["foundation model", "ocean"]

  ship_trajectories:
    queries:
      - ["ship trajectories", "deep learning"]
      - ["ship trajectories", "machine learning"]
      - ["vessel tracking", "AIS"]
      - ["fishing", "vessel"]

  fish_plankton:
    queries:
      - ["chlorophyll", "satellite"]
      - ["species distribution model"]

  geo_reasoning:
    queries:
      - ["agent", "geospatial"]
      - ["geospatial", "reasoning"]
      - ["geospatial", "LLM"]
      - ["geospatial", "VLM"]
      - ["earh", "query"]
      - ["geoAI"]

